# Project Ascend — mobile

The Flutter client. For setup, prerequisites, environment configuration, and running the app
against the backend, see the [repository root README](../../README.md) — this package doesn't
have separate setup steps.

For the Flutter/Dart-specific commands only:

```bash
flutter pub get
flutter analyze
flutter test
dart format --output=none --set-exit-if-changed .
```
