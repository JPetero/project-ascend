/**
 * Single source of Ascend Promote's numeric limits — Founder Scenario
 * 23: "frequency limits cap how often the same user sees promoted
 * content," centrally configurable rather than hard-coded per-widget,
 * same pattern as `trainer-group-policy.ts`.
 */
export const PROMOTE_MAX_IMPRESSIONS_PER_VIEWER_PER_DAY = 3;
